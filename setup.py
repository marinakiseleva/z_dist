from setuptools import setup, find_packages
from distutils.core import setup

setup(name='z_evaluator',
      packages=find_packages(),
      version='2.4',
      description='Tools to compare and evaluate the redshift distributions of transient clases in the PLASTICCS and THEx datasets.',
      author='Marina Kisley',
      author_email='marinaki@email.arizona.edu'
      )
